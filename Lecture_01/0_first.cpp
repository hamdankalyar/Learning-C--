#include <iostream>
using namespace std;

/*
 * iostream is a standard library that allows C++ programs to perform input and output operations.
 
 *
 * For example, with `using namespace std;`, you can write `cout` instead of `std::cout` to print to the console.
 */
int main(){
    
    cout << "hello world" << endl;
    // std::cout << "Hello" << std::endl;


    // whatever code 
    return 0; 
}
